<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta charset="UTF-8">
        <title>{% block title %}Witches Beauty{% endblock %}</title>
        {% block stylesheets %}{{ encore_entry_link_tags('app') }}{% endblock %}
        {% block javascripts %}{{ encore_entry_script_tags('app') }}{% endblock %}
    </head>
    <body>
      <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
          <a href="{{path('witches_beauty_home')}}" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
            <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"/></svg>
            <span class="fs-5 fw-semibold">Witches Beauty</span>
          </a>
          <ul class="nav nav-pills">
          <li class="nav-item">
            <a href="{{path('witches_beauty_home')}}" style="text-decoration:none"> <input type="button" value='Home' class="btn btn-toggle d-inline-flex align-items-center rounded border-0 collapsed"></a>
          </li>
          <li class="nav-item">
            <button class="btn btn-toggle d-inline-flex align-items-center rounded border-0 collapsed" data-bs-toggle="collapse" data-bs-target="#dashboard-collapse" aria-expanded="false">
              Product
            </button>
            <div class="collapse" id="dashboard-collapse">
              <ul class="btn-toggle-nav list-unstyled fw-normal pb-1 small">
                <li><a href="{{path('witches_beauty_product_face')}}" class="link-dark d-inline-flex text-decoration-none rounded">Face</a></li>
                <li><a href="{{path('witches_beauty_product_eyes')}}" class="link-dark d-inline-flex text-decoration-none rounded">Eyes</a></li>
                <li><a href="{{path('witches_beauty_product_lips')}}" class="link-dark d-inline-flex text-decoration-none rounded">Lips</a></li>
              </ul>
            </div>
          </li>
          <li class="nav-item">
            <a href="{{path('witches_beauty_about')}}" style="text-decoration:none"> <input type="button" value='About Us' class="btn btn-toggle d-inline-flex align-items-center rounded border-0 collapsed"></a>
          </li>
        </ul>
      </header>
      </div>

      {% block body %}{% endblock %}


      <div class="container">
        <footer class="py-3 my-4">
          <ul class="nav justify-content-center border-bottom pb-3 mb-3">
            <li class="nav-item">
              <a href="{{path('witches_beauty_home')}}" class="nav-link px-2 text-muted">Home</a>
            </li>
            <li class="nav-item">
              <a href="#" class="nav-link px-2 text-muted">Product</a>
                <ul class="btn-toggle-nav list-unstyled fw-normal pb-1 small">
                  <li><a href="{{path('witches_beauty_product_face')}}" class="nav-link px-2 text-muted">Face</a></li>
                  <li><a href="{{path('witches_beauty_product_eyes')}}" class="nav-link px-2 text-muted">Eyes</a></li>
                  <li><a href="{{path('witches_beauty_product_lips')}}" class="nav-link px-2 text-muted">Lips</a></li>
                </ul>
            </li>
            <li class="nav-item">
              <a href="{{path('witches_beauty_about')}}" class="nav-link px-2 text-muted">About Us</a>
            </li>
          </ul>
          <p class="text-center text-muted">&copy; Witches Beauty</p>
        </footer>
      </div>

<div class="b-example-divider"></div>

    </body>
</html>
